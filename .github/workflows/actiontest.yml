name: "Welcome github action workflow"
on:
  pull_request:
    branches: main
  
jobs:
  build:
    runs-on : ubuntu-latest
    steps:
     - name: run an action
       run: echo "Hello Team"
      
  
